# 项目目录约定

> AI 进项目第二个读的文件。定义本项目文件存放规则。
> 本文件在阶段三根据确定的技术栈后重新生成。

## 代码

> ⚠️ 以下为建议结构，阶段三确认技术栈后最终确定

```
[项目根目录]/
├── miniprogram/           ← 微信小程序前端代码
│   ├── pages/             ← 页面目录
│   │   ├── index/         ← 首页（咨询方向展示）
│   │   ├── booking/       ← 预约表单
│   │   └── mine/          ← "我的"页面
│   ├── components/        ← 公共组件
│   ├── utils/             ← 工具函数
│   └── app.js / app.json / app.wxss
├── admin/                 ← 后台管理系统前端（Web）
│   ├── src/
│   │   ├── pages/         ← 页面
│   │   ├── components/    ← 公共组件
│   │   └── api/           ← API 请求层
│   └── ...
├── server/                ← 后端服务
│   ├── src/
│   │   ├── routes/        ← 路由
│   │   ├── controllers/   ← 控制器
│   │   ├── models/        ← 数据模型
│   │   ├── middleware/     ← 中间件
│   │   └── utils/         ← 工具函数
│   └── ...
```

## 文档
- **需求**: [docs/requirements.md]
- **设计规范**: [designs/design-tokens.md]
- **页面 HTML**: [designs/]
- **模块 md**: 与对应代码文件同目录

## 测试
- **测试代码**: [待阶段三确定]
- **测试报告**: [docs/test-report.md]
