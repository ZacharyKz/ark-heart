# CLAUDE.md — 方舟之心 The Ark Heart

> AI 进项目第一个读的文件。**地图第一，规则第二。** 不重复 project.md 的内容。

---

## 一、信息发现策略

按以下顺序获取上下文，不提前规划"该读什么"：

```
1. 读本文件（CLAUDE.md） → 知道去哪找什么
2. 读 .project-convention.md → 知道文件放哪
3. 读 project.md → 了解项目全貌、技术栈、阶段状态
4. 按任务自然探索具体文件
```

**模块接口信息**：优先读对应模块的模块 md（与代码同目录），**不优先读源码**。
如果读完模块 md 作答，**必须告知用户**：
> "我读了模块 X 的模块 md，没有读具体代码。如果需要我分析代码细节，请告诉我。"

---

## 二、项目事实

| 你需要知道 | 去这里 |
|-----------|--------|
| 项目全貌、技术栈、阶段状态 | [`project.md`](./project.md) |
| 代码/文档/测试的存放规则 | [`.project-convention.md`](./.project-convention.md) |
| 各模块接口文档 | 与对应代码同目录的 `模块名.md` |

### 常用命令

> ⚠️ 技术栈待阶段三确定后填写

```
开发启动：          [待填]
构建：              [待填]
测试（全量）：      [待填]
测试（单文件）：    [待填]
类型检查：          [待填]
数据库迁移：        [待填]
```

### 环境变量

> ⚠️ 技术栈待阶段三确定后填写

```
[待填]
```

---

## 三、团队约束

### 代码风格
- 遵循项目现有代码风格，不引入新的模式
- 公共组件/工具出现两次及以上 → 抽取复用
- 不引入新的依赖/UI 库（除非技术方案明确需要）

### 协作约定
- **单人模式**：无分支/PR 约束，直接在主线开发

### 禁止事项
- 不改动无关文件
- 不写 speculative 代码（"可能以后会用"的功能）
- 不改接口/字段而不同步更新模块 md

---

## 四、任务规则

### 开始任务之前
1. 如果任务描述模糊，**先列出你的 assumptions** 让我确认
2. 涉及多文件/跨模块的变更，**先提计划再动手**

### Plan 格式
```
影响范围：修改/新增几个文件
核心逻辑：一句话说清怎么做
风险：有没有破坏性变更
```

---

## 五、范围控制

- **最小改动原则**：只改任务目标要求的文件，不碰无关代码
- 遇到代码质量问题（死代码、可优化点）→ 记下来但不修，除非明确要求
- 不写"以后可能会用"的扩展点

---

## 六、验证闭环

### 完成一个任务后
1. 按风险分级执行对应测试层级
2. 确保类型检查无报错
3. 确保 dev server / 构建无报错
4. **如果构建/测试失败 → 自修复直到通过**，修复过程记录在变更中
5. `test-report.md` 必须含具体证据和风险关注清单

### 验收标准
- [ ] 构建/启动无报错
- [ ] 类型检查通过
- [ ] 改动仅限于目标文件
- [ ] 接口/字段变更已更新模块 md
- [ ] 测试通过（如果有）

---

## 七、维护机制

本文件本身需要维护：
- 项目事实变化（改技术栈、加命令、换数据库）→ **同步更新本文件**
- 每轮项目回顾时检查本文件是否过时

---

## 八、AI 主动引导（供参考）

### 需求引导清单
1. 权限 — 谁可以用？未登录/无权限看到什么？
2. 数据边界 — 最大值/最小值/空值/特殊字符？
3. 时间状态 — 过期/超时？状态流转？
4. 异常路径 — 网络异常？并发操作？
5. 配套功能 — 撤回/通知/记录？
6. 范围 — V1做什么？明确不做什么？

### 设计引导清单
1. 状态覆盖 — 空/加载/错误/成功态？
2. 极端数据 — 长文本/大金额/图片失败？
3. 交互细节 — 防抖/必填标注/二次确认？
4. 多端适配 — 触控区≥44px/横竖屏？
